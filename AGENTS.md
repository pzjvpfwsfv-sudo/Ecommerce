# 项目工作约定

## 独立判断与自检

- 用户负责目标和取舍，助手负责技术判断；用户同意某个方案不等于方案已经正确。
- 改动前读取现有实现，检查是否复用即可、是否破坏旧链路、是否超出当前阶段，以及数据真实性和资源预算是否成立。发现更简单可靠的方案应主动建议，不盲目顺从。
- 重要接口和架构保持设计确认；已有明确授权的常规执行不重复询问。不要为追求全面不断增加组件、章节或重写现有系统。
- 交付前核对实现与验收证据，区分单元测试、真实数据验证和整条链路验证；未测项目不得标为完成。仅文档变更无需反复运行全部测试。
- 去重必须说明状态有效期、跨回放和存储幂等边界；不得把 Kafka 发送确认、Checkpoint 或有限时间去重单独宣称为端到端 exactly-once。
- 人工测试夹具只能用于边界验证；真实历史回放不能宣传为企业实时生产数据。缺失业务字段保留缺失，不编造。

## 本机存储

- 默认开发工作区为 `.worktrees/chapter-10-controlled-tools`；不要覆盖主工作区的用户修改。
- 新数据、构建产物、测试临时文件、下载和依赖缓存不得主动写入 C 盘。工作区内数据与报告继续使用已忽略的 `data/`、`tmp/`，它们位于 D 盘。
- 执行本项目 Python 测试和下载命令前，为当前进程设置 `TEMP`、`TMP` 为 `D:\EcommerceDev\temp`，`PIP_CACHE_DIR` 为 `D:\EcommerceDev\cache\pip`。不修改用户或系统全局环境。
- 测试临时目录必须在 `.worktrees` 之外，否则旧迁移测试的主仓库路径保护会拒绝它。
- 原有 Java 构建优先使用 `scripts/build_chapter_9_datastream.ps1` 的 Docker Java 17 环境。Docker 数据已在 `D:\DockerData\wsl`，不要重复迁移；新增原生 Maven/npm 等工具调用前，也须显式将其缓存和临时目录放到非 C 盘。
- Windows 和 Docker Desktop 自身的小型运行配置、日志不保证完全离开 C 盘。不得因此擅自移动用户目录、全局 TEMP、凭据库或系统文件；C 盘余量不足时不得盲目扩大运行规模。
- 不自动清理用户文件、镜像、数据卷或恢复出厂设置。存储迁移先核对真实位置、容量、停止条件与备份，再处理。

## GitHub 与认证

- 使用已有 `origin`：`https://github.com/pzjvpfwsfv-sudo/Ecommerce.git`，备份到当前开发分支，不重复创建仓库，不强推或擅自合并 main。
- 本机 Windows 在 2026-09-18 启用了 `127.0.0.1:7890` 代理。Git 直接连接失败时，先核对当前系统代理，再按命令传入 `-c http.proxy=http://127.0.0.1:7890`；不将代理永久写入仓库配置，也不关闭 TLS 校验。
- 浏览器登录状态不等于 Git 命令行已经认证；先区分网络失败和认证失败。需要登录时让用户在官方流程里输入账号、扫码或确认授权，不读取浏览器 cookie 或索要密码/token。
- 推送成功后比较远端分支 SHA 和本地 HEAD；原始数据、临时报告、密钥与 Docker 数据卷不属于 GitHub 代码备份。
